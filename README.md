Урок Glo Academy
Корзина на JS.

Знакомство с webpack

копия index.js -- упрощенный вариант кода:

getData().then((data) => {
    renderCards(data);
    renderCatalog();
    toggleCheckbox();
    toggleCart();
    addCart();
    actionPage();
});